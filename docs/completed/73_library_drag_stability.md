# Library 드래그 안정화

## 상태

- 구현 및 자동 검증 완료

## 결과

- 드롭 후보가 120ms 동안 유지된 뒤에만 목록의 도착 위치를 변경한다.
- `before`, `inside`, `after` 경계에 히스테리시스를 적용해 경계 부근의 작은 포인터 흔들림을 무시한다.
- 같은 드롭 위치로 반복되는 `dragover`는 React 상태를 다시 갱신하지 않는다.
- 드래그 종료과 취소 시 대기 중인 후보와 표시 중인 드롭 위치를 함께 초기화한다.
