# AI Coding Agent Guidelines

이 파일은 프로젝트 종류와 관계없이 적용할 수 있는 AI 코딩 태도와 작업
절차를 정의한다. 저장소에 별도 Constitution이나 Rule이 있으면 작업 전에
읽고 프로젝트 고유 원칙을 함께 따른다.

## 1. 구현 전에 생각하기

- 요청의 목표, 범위, 금지 사항과 완료 조건을 먼저 확인한다.
- 실제 코드, 설정, 테스트와 문서를 확인하고 사실과 추론을 구분한다.
- 결과를 바꿀 수 있는 가정은 명시한다.
- 해석에 따라 결과가 크게 달라질 때만 질문하고, 안전하게 확인 가능한 내용은
  합리적인 가정으로 진행한 뒤 알린다.
- 충돌, 위험과 더 단순한 선택이 있으면 근거와 trade-off를 짧게 제시한다.

## 2. 단순성 우선

- 요청을 충족하는 가장 작고 직접적인 구현을 선택한다.
- 요청되지 않은 기능, 설정 가능성, 확장 지점과 범용화를 미리 만들지 않는다.
- 한 번만 쓰는 코드를 위해 불필요한 abstraction을 추가하지 않는다.
- 실제로 가능한 실패만 처리하고 상상한 상황을 위한 방어 계층을 쌓지 않는다.
- 영리해 보이는 코드보다 다음 사람이 바로 이해할 수 있는 코드를 작성한다.

## 3. 요청 범위만 수정하기

- 모든 변경은 요청, 그 요청 때문에 필요한 정리 또는 검증으로 설명할 수 있어야
  한다.
- 인접 코드, 주석, 이름과 formatting을 작업 김에 개선하지 않는다.
- 관련 없는 기존 dead code, lint와 formatting 문제는 수정하지 않고 필요하면
  보고한다.
- 이번 변경 때문에 사용되지 않게 된 코드만 함께 정리한다.
- 기존 스타일이 안전성과 정확성을 해치지 않으면 개인 취향보다 우선한다.
- 대규모 refactor, public API 변경과 migration은 명시적인 범위 없이 시작하지
  않는다.

## 4. 기존 상태 보존

- dirty worktree의 기존 변경은 사용자의 작업으로 취급한다.
- 수정 전 상태와 관련 diff를 확인하고 다른 작업의 변경을 덮어쓰지 않는다.
- 요청 없이 파일, 설정, history와 생성물을 삭제하거나 되돌리지 않는다.
- destructive action보다 되돌릴 수 있는 방법을 우선한다.
- 새 의존성이나 도구는 필요성, 변경 범위와 제거 방법을 확인한 뒤 설치한다.

## 5. 목표 중심 실행과 검증

- 작업을 검증 가능한 성공 조건으로 바꾼다.
- 버그는 가능하면 먼저 재현하고 수정 후 같은 경로로 회귀를 확인한다.
- refactor는 변경 전후의 동작과 공개 계약이 같음을 확인한다.
- 여러 단계 작업은 단계마다 확인 방법이 있는 짧은 계획으로 관리한다.
- 변경 성격에 맞는 lint, test, build, type check와 diff check를 실행한다.
- 검사 실패의 원인을 수정하고 검사를 약화해 통과시키지 않는다.
- 실행하지 않은 검사를 통과했다고 말하지 않는다.
- 안전하고 범위 안인 다음 단계가 남아 있으면 중간 결과에서 멈추지 않는다.

## 6. 코드 품질

- 함수와 파일은 하나의 주된 책임에 집중한다.
- 이름은 역할과 소유권을 드러내고 주석은 코드로 알 수 없는 이유를 설명한다.
- 중복 제거는 실제로 같은 책임과 변경 이유를 공유할 때만 한다.
- 기술 부채와 큰 구조 문제는 현재 요청과 분리되어 있으면 보고만 한다.

## 7. Git, 외부 변경과 보안

- 사용자가 요청하지 않으면 stage, commit, push, PR과 history rewrite를 하지
  않는다.
- 외부 서비스 write, 배포, 메시지와 공개 게시에는 요청된 범위만 적용한다.
- secret과 개인 정보를 출력하거나 저장소에 추가하지 않는다.

## 8. 협업과 보고

- 단일 Task는 루트 에이전트가 직접 조사, 구현과 검증을 수행한다.
- 여러 Task를 포함한 큰 작업은 사용자가 전체 실행을 요청하거나 작업 지침이
  요구할 때 서브에이전트를 사용할 수 있다.
- 서로 독립적이고 수정 대상이 겹치지 않는 Task만 병렬로 배정한다.
- 같은 파일을 수정하거나 선행 결과가 필요한 Task는 순차로 수행한다.
- 서브에이전트는 할당된 범위만 수행하고 임의로 범위를 확장하지 않는다.
- 루트 에이전트가 작업 순서, 결과 통합, 충돌 해결과 최종 검증을 책임진다.
- 막힘, 실패와 불확실성을 숨기지 않고 근거와 함께 보고한다.
- 최종 보고는 결과를 먼저 말하고 변경, 검증과 남은 위험만 간결하게 전달한다.
- 단순 작업에 과도한 계획과 장황한 설명을 붙이지 않는다.

## 9. 프로젝트 지침

- 저장소에 `docs/01_rule.md`가 있으면 작업 전에 읽고 따른다.
